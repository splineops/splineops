name: Development benchmarks

on:
  push:
    branches: [feature/publication]
    paths:
      - ".github/workflows/tensorspline-benchmark.yml"
      - "benchmarks/consolidation-thresholds.json"
      - "scripts/benchmark_batch_scaling.py"
      - "scripts/benchmark_downstream_workflows.py"
      - "scripts/benchmark_workflows.py"
      - "scripts/profile_affine_phases.py"
      - "src/splineops/affine/**"
      - "src/splineops/differentials/**"
      - "src/splineops/multiscale/wavelets/**"
      - "src/splineops/spline_interpolation/**"
  workflow_dispatch:
    inputs:
      profile:
        description: "benchmark profile"
        required: true
        default: "standard"
        type: choice
        options: [smoke, standard]

jobs:
  benchmark:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    runs-on: ${{ matrix.os }}
    env:
      BENCHMARK_PROFILE: ${{ inputs.profile || (contains(github.event.head_commit.message, '[standard-bench]') && 'standard') || 'smoke' }}
      OMP_NUM_THREADS: 1
      OPENBLAS_NUM_THREADS: 1
      MKL_NUM_THREADS: 1
      NUMEXPR_NUM_THREADS: 1

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install package
        run: python -m pip install -e .

      - name: Benchmark construction, evaluation, and peak memory
        run: >-
          python scripts/benchmark_tensorspline.py
          --profile "${{ env.BENCHMARK_PROFILE }}"
          --output-json tensorspline.json
          --output-csv tensorspline.csv

      - name: Benchmark TensorSpline memory scaling
        run: >-
          python scripts/benchmark_tensorspline_memory_scaling.py
          --output-json tensorspline-memory.json
          --output-csv tensorspline-memory.csv

      - name: Benchmark repeated TensorSpline coordinates
        run: >-
          python scripts/benchmark_tensorspline_query_plan.py
          --output-json tensorspline-query-plan.json
          --output-csv tensorspline-query-plan.csv

      - name: Benchmark equivalent affine rotation
        run: >-
          python scripts/benchmark_affine.py
          --profile "${{ env.BENCHMARK_PROFILE }}"
          --output-json affine.json
          --output-csv affine.csv

      - name: Profile complete affine execution phases
        run: >-
          python scripts/profile_affine_phases.py
          --profile "${{ env.BENCHMARK_PROFILE }}"
          --output-json affine-phases.json
          --output-csv affine-phases.csv

      - name: Benchmark vectorized differentials
        run: >-
          python scripts/benchmark_differentials.py
          --profile "${{ env.BENCHMARK_PROFILE }}"
          --output-json differentials.json
          --output-csv differentials.csv

      - name: Benchmark vectorized multiscale transforms
        run: >-
          python scripts/benchmark_multiscale.py
          --profile "${{ env.BENCHMARK_PROFILE }}"
          --output-json multiscale.json
          --output-csv multiscale.csv

      - name: Benchmark complete reusable workflows
        run: >-
          python scripts/benchmark_workflows.py
          --profile "${{ env.BENCHMARK_PROFILE }}"
          --output-json workflows.json
          --output-csv workflows.csv

      - name: Benchmark explicit batch memory scaling
        run: >-
          python scripts/benchmark_batch_scaling.py
          --profile "${{ env.BENCHMARK_PROFILE }}"
          --output-json batch-scaling.json
          --output-csv batch-scaling.csv

      - name: Benchmark realistic downstream workflows
        run: >-
          python scripts/benchmark_downstream_workflows.py
          --profile "${{ env.BENCHMARK_PROFILE }}"
          --output-json downstream-workflows.json
          --output-csv downstream-workflows.csv

      - name: Check machine-relative regression thresholds
        run: >-
          python scripts/check_benchmark_thresholds.py
          --policy benchmarks/consolidation-thresholds.json
          --artifacts-dir .

      - uses: actions/upload-artifact@v6
        with:
          name: development-benchmarks-${{ runner.os }}-${{ env.BENCHMARK_PROFILE }}-${{ github.sha }}
          path: |
            tensorspline.json
            tensorspline.csv
            tensorspline-memory.json
            tensorspline-memory.csv
            tensorspline-query-plan.json
            tensorspline-query-plan.csv
            affine.json
            affine.csv
            affine-phases.json
            affine-phases.csv
            differentials.json
            differentials.csv
            multiscale.json
            multiscale.csv
            workflows.json
            workflows.csv
            batch-scaling.json
            batch-scaling.csv
            downstream-workflows.json
            downstream-workflows.csv
          if-no-files-found: error
